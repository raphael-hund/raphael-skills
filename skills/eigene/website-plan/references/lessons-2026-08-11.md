# Session-Lektionen website-plan (2026-08-11)

Quellen: Sessions `78d14179` (BRAUN), `c66ee428` (Sorglos). Kein Freitext-History-Wiedereinbau in Plans.

| Beobachteter Fehler | Pflichtverhalten |
|---|---|
| Freestyle-JS (`braun-…`, `sorglos-…`) statt Kanon | Nur Templates aus `references/`; nur Platzhalter füllen |
| Enger Scope als „voller website-plan fertig“ | `SCOPE` explizit; Delta in `00-meta-plan.md`; FULL-DoD nicht behaupten |
| Cockpit medium / Fable-Worker | Cockpit Ultracode/max; **nie** `agentType: fable*` |
| G-IA/G-DESIGN ohne Stop | Hard stop `AWAITING_USER` / `await_user` — kein stilles Defaulten |
| AskUserQuestion-Kaskade vor Workflow | Defaults setzen; Workflow starten; Gates nur G-IA/G-DESIGN |
| User will Fake-Reviews | Ablehnen; Review-Slots + PLACEHOLDER; nie erfundene Proofs |
| „fertig“ ohne `04-seo-plan` bei FULL | DoD-Checkliste; fehlende Datei = nicht done |
| Run-ID fehlt, Parent schreibt parallel | Ohne Run-ID = nicht gestartet; Parent watched, schreibt nicht parallel in dieselben Dateien |

## Session-Lektionen 2026-08-12

Quellen: `116590ab-267b-4d35-bb84-fe8bcb98f819` (WILHELM),
`5c51f3a5-6213-471d-a5e6-98120e39f4f2` (Sorglos).

| Beobachteter Fehler / gutes Muster | Pflichtverhalten |
|---|---|
| WILHELM machte erst am offenen Design-Gate 3 separat nutzbare GPT-Prompts | `gpt-prompts.md` ist Pflichtoutput von Lauf 2; je Richtung ein eigenständiger Prompt |
| WILHELM-Prompts hatten Stilideen, aber keine recherchierten Screenshot-Attachments | Vor Prompting reale Regionen capturen, manifestieren und pro Prompt mappen |
| Sorglos war unverständlich, bis jede Seite/Bildart inventarisiert war | Pro Seite/Sektion `VORHANDEN \| ERSTELLEN \| SUCHEN \| FOTOGRAFIEREN \| GESPERRT` eindeutig setzen |
| Sorglos plante KI-Nachher zu echten Vorher-Fotos | Nur als klar bezeichnete Visualisierung; nie als Ergebnis-/Proof-Paar |
| Datei-DoD ließ Briefs als fertige Mockups durchgehen | Captures, Prompts und generierte Vergleichsbilder getrennt prüfen |
| Read-only Critics sollten Dateien schreiben; Parent musste Journal-Inhalte nachziehen | Schreiboutput nur an Writer oder separaten Persist-Schritt vergeben |
| Copy-Draft und Humanizer liefen auf denselben Dateien parallel | Draft abschließen, danach Humanizer sequenziell |

| **TEMPO** Opus für Asset-Inventar / Massen-Read (Sorglos: 5–11 Min/Welle, 9–14 MB Logs) | Assets = `luna-worker` in Shards + Luna-Merge; nie `opus-builder`/`sonnet-worker` für Inventar |
| **TEMPO** Ein Opus-Humanizer über alle Seiten (Sorglos: 44 Min, 157 Tools, 247 Turns) | Humanizer parallel in 4 Shards mit `kimi-worker`; max 3 Tools/Datei, ein Pass |
| **TEMPO** Bild-Gen: 2 Agents à 5 Bilder sequentiell + `--wait-timeout 10m` (~32 Min/Agent) | 1 Bild pro Agent, max 5 parallel; Wait ≤3–5 Min; Retry nur Fails |
| Freestyle-Script mit `opus-builder` auf Mechanik-Rollen | Nur Kanon-Templates; agentType aus SPEED-Tabelle (roles/STANDALONE) |
| Parallel-Faktor ~1.5 trotz `parallel()` | Phasen mit Shards parallelisieren; sequenzielle Barriers nur wo Datei-Konflikt |

